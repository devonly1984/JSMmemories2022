import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const toolkit = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = toolkit.actions

export default toolkit.reducer